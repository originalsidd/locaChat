import React from 'react';
import Logo from '../assets/load.svg';

const Load = () => {
    return (
        <div className='load'>
            <img src={Logo} className='logo' />
        </div>
    );
};

export default Load;
